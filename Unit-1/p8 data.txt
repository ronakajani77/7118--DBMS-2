-- 1. Drop the existing table to clear out bad/duplicate data
DROP TABLE EMP PURGE;

-- 2. Create the table fresh with an explicit Primary Key on EID
CREATE TABLE EMP (
    EID NUMBER PRIMARY KEY,
    EName VARCHAR2(50),
    Deptno NUMBER,
    Deptname VARCHAR2(50),
    Gender CHAR(1),
    Age NUMBER(3),
    BasicSal NUMBER(10, 2)
);

-- 3. Insert fresh, clean data (each EID is unique)
INSERT INTO EMP (EID, EName, Deptno, Deptname, Gender, Age, BasicSal) 
VALUES (101, 'Sarah Jenkins', 10, 'IT', 'F', 29, 60000.00);

INSERT INTO EMP (EID, EName, Deptno, Deptname, Gender, Age, BasicSal) 
VALUES (102, 'Michael Chang', 20, 'Finance', 'M', 35, 75000.00);

INSERT INTO EMP (EID, EName, Deptno, Deptname, Gender, Age, BasicSal) 
VALUES (103, 'Elena Rostova', 10, 'IT', 'F', 31, 50000.00);

-- 4. Commit changes to save them to the database
COMMIT;